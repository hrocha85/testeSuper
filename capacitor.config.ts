import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.testeSuper',
  appName: 'Teste Super',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
